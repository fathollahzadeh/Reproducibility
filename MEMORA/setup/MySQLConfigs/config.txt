[mysqld]
# Memory and buffers
innodb_buffer_pool_size = 100G         # 65-70% of total RAM for InnoDB
innodb_redo_log_capacity = 8G
#innodb_log_file_size = 4G              # Large to reduce checkpointing overhead
innodb_log_buffer_size = 256M          # Helps with large transactions
innodb_flush_log_at_trx_commit = 2     # Better bulk insert performance (less durability)
innodb_io_capacity = 1000              # Adjust depending on your disk
innodb_io_capacity_max = 2000

# Bulk insert optimization
bulk_insert_buffer_size = 512M
max_allowed_packet = 1G
read_buffer_size = 64M
read_rnd_buffer_size = 128M
sort_buffer_size = 64M

# Disable binary logging (optional if not needed)
skip-log-bin

# Load data performance
local_infile = 1

# Disable slow query log during import
slow_query_log = 0


############################
# Unistall:
sudo systemctl stop mysql
sudo apt-get purge mysql-server mysql-client mysql-common mysql-server-core-* mysql-client-core-*
sudo rm -rf /etc/mysql /var/lib/mysql
sudo apt autoremove
sudo apt autoclean
